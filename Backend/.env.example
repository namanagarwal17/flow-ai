# Server port (Render sets this automatically)
PORT=4000

# Comma-separated list of allowed frontend origins
# Leave empty to allow all (dev mode)
# Example for production: ALLOWED_ORIGINS=https://your-flowai.vercel.app
ALLOWED_ORIGINS=
